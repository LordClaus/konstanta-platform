# Development & test dependencies (installed on top of requirements.txt).
-r requirements.txt

pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0          # ASGI transport for end-to-end API tests
ruff>=0.5.0            # linter / formatter (matches the CI gate)
