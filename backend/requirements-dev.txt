# Development & test dependencies (installed on top of requirements.txt).
-r requirements.txt

pytest>=9.1.1
pytest-asyncio>=1.4.0
pytest-cov>=7.1.0      # coverage measurement (CI gate: --cov-fail-under)
httpx>=0.28.1          # ASGI transport for end-to-end API tests
ruff>=0.15.20            # linter / formatter (matches the CI gate)
mypy>=2.1.0           # static type checking (CI gate)
