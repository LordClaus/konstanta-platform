# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-multipart>=0.0.9

# ── Database: async SQLAlchemy 2.0 + Alembic ──────────────────────────────────
SQLAlchemy[asyncio]>=2.0.30
alembic>=1.13.0
asyncpg>=0.29.0        # PostgreSQL async driver (production / docker-compose)
aiosqlite>=0.20.0      # SQLite async driver (local dev / tests)

# ── Auth (JWT, password hashing, Google OAuth verification) ───────────────────
pyjwt>=2.9.0
bcrypt>=4.0.0
google-auth>=2.30.0

# ── AI providers + email + outbound HTTP ──────────────────────────────────────
aiohttp>=3.9.0

# ── Telegram bot (in-process; optional, enabled by BOT_TOKEN) ─────────────────
aiogram>=3.7.0

# ── Image upload → Cloudflare R2 (S3-compatible) ──────────────────────────────
Pillow>=10.3.0
boto3>=1.34.0

# ── Rate limiting (brute-force protection on auth routes) ─────────────────────
slowapi>=0.1.9
