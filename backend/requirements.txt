# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.138.2
uvicorn[standard]>=0.49.0
pydantic>=2.13.4
pydantic-settings>=2.14.2
python-multipart>=0.0.32

# ── Database: async SQLAlchemy 2.0 + Alembic ──────────────────────────────────
SQLAlchemy[asyncio]>=2.0.51
alembic>=1.18.5
asyncpg>=0.31.0        # PostgreSQL async driver (production / docker-compose)
aiosqlite>=0.22.1      # SQLite async driver (local dev / tests)

# ── Auth (JWT, password hashing, Google OAuth verification) ───────────────────
pyjwt>=2.13.0
bcrypt>=5.0.0
google-auth>=2.55.1

# ── AI providers + email + outbound HTTP ──────────────────────────────────────
aiohttp>=3.14.1

# ── Telegram bot (in-process; optional, enabled by BOT_TOKEN) ─────────────────
aiogram>=3.29.0

# ── Image upload → Cloudflare R2 (S3-compatible) ──────────────────────────────
Pillow>=12.2.0
boto3>=1.43.37

# ── Rate limiting (brute-force protection on auth routes) ─────────────────────
slowapi>=0.1.10
